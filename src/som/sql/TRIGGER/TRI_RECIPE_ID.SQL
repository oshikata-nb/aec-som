CREATE OR REPLACE TRIGGER TRI_RECIPE_ID
BEFORE INSERT 
ON RECIPE_HEADER
REFERENCING OLD AS OLD NEW AS NEW
FOR EACH ROW 
DECLARE
        NEWID RECIPE_HEADER.RECIPE_ID%TYPE;
BEGIN
	select nokey_recipe.nextval into NEWID from dual;
 	:new.recipe_id := NEWID;
END;
/
